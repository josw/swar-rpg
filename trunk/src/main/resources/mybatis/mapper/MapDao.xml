<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.game.swar.map.dao.MapDao">

    <select id="getMap" resultType="com.game.swar.map.model.Map" parameterType="java.lang.String">
        select X(location) X, Y(location) Y, type, amount, owner from map
    </select>

</mapper>